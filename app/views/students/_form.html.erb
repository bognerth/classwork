<%= form_for(@student, remote: true) do  %>
  <%= hidden_field_tag :teststudent_id, @teststudent.id %>
  <%= text_field_tag :points, @teststudent.points, :size => "5" %>
  <%= submit_tag 'OK' %>
<% end %>